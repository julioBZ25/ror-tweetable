<!DOCTYPE html>
<html>
  <head>
    <title>Tweetable</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="header">
      <nav class="nav--log">
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
        <% if user_signed_in? %>
          <div>
            <%= link_to "edit profile", edit_user_registration_path(current_user), class: 'link_to' %>
          </div>
          <div>
            <%= link_to "Log out", destroy_user_session_path, class: 'link_to', data: { turbo_method: :delete } %>
          </div>
        <% else %>
          <%= link_to "Log in", new_user_session_path %>
        <% end %>
      </nav>
    </header>
    <%= yield %>
  </body>
</html>
